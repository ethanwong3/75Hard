import { View, Text, StyleSheet, TouchableOpacity, Image, Pressable } from 'react-native';


export default function NewUser({onPress}) {
    return (
        <Pressable>
            <View>
                <Text>New User? <Text>Sign Up</Text></Text>
            </View>
        </Pressable>
 
    );

}

const styles = StyleSheet.create({
    example: {

    },
});